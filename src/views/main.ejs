<h3>Hello Pikka</h3> 

<% for(let i = 0; i < photos.length; i++){ %>
  <% if((i % 4) === 0 || i === 0) { %>
    <div class="row"> 
  <% } %> 
    <div class="column">
      <img src=<%- `/-/images/${photos[i].id}` %> alt="Norway" style="width:100%"> 
    </div>
  <% if(((i % 4) === 0 || i === photos.length) && i !== 0) { %>
    </div> 
  <% } %>   
<% } %> 